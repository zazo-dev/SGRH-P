@model IEnumerable<SGRH.Web.Models.Entities.AbsenceCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Category_Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Category_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Absence_Category" class="btn btn-info">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Absence_Category" class="btn btn-sencondary">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Absence_Category" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
